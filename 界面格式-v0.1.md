# 国标算番器 —— 界面与展示格式 v0.1

## 一、布局：上→下三段

### ① 已选手牌区（顶部）
横向两栏：
- **左栏 = 副露区**：吃／碰／杠，每组面子一块，按录入顺序排；杠标明杠或暗杠。
- **右栏 = 手牌区**：暗牌，按花色・点数排序。
每张牌显示牌面；角落显示当前手牌总数。

**副露录入流程**：副露区顶部四个按钮——`吃` `碰` `明杠` `暗杠`；先选类型，再点右侧牌库的牌 → 加入副露区。按定义校验：
- 吃 = 同花色 3 张连续顺子
- 碰 = 3 张相同
- 明杠 = 4 张相同（明）
- 暗杠 = 4 张相同（暗）
不合定义则拒绝（提示）。

### ② 牌库点击区 + 圈门设定区
牌库区（34 种牌全部列出：万 1-9、条 1-9、筒 1-9、东南西北、中发白）：
- 点击 → 加入手牌（暗牌）。
- 每张牌 **手牌 + 副露合计上限 4 张**；满 4 张该牌置灰不可再点。
- 每张牌角标显示已用张数（如 `2/4`）。

圈门设定区（放在牌库区**右侧**）：
- **圈风**：东／南／西／北 单选。
- **门风**：东／南／西／北 单选。
- 用途：`圈风刻`、`门风刻` 各 2 番的判定，缺了它风牌刻子这两番出不来。

### ③ 结果区（和牌张 + 番数）
列出所有**结构成和**的听牌张。每张横排四栏：

```
点和 X番 ｜ 自摸 X番 ｜ 点和绝张 X番 ｜ 自摸绝张 X番
```

- 不做 8 番过滤，**全部显示**；≥8 番高亮，<8 常规灰显。
- **番种明细**：每张和牌张都列出明细，只列牌型番；和牌方式类（门前清／不求人／自摸／和绝张）不重复列（已并入四栏数字）。若为无番和，明细只显示"无番和"。
- 排序：无要求，正常显示即可。

## 二、番数核算口径
设 `base` = 手牌番（含互斥扣减；不含门清类、和牌方式类）：

- 点和     = base + (门清 ? 2 : 0)          # 门前清
- 自摸     = base + (门清 ? 4 : 1)          # 不求人 / 自摸
- 点和绝张 = base + (门清 ? 2 : 0) + 4       # 门前清 + 和绝张
- 自摸绝张 = base + (门清 ? 4 : 1) + 4       # 不求人/自摸 + 和绝张
- **无番和**：base=0 且副露 且该方式不引入番 → 点和栏记 8；其余栏因引入番，不足 8。

## 三、口径约定
- **门清** = 无吃／碰／明杠。**暗杠不算破门清**（门前清/不求人的定义只排除吃、碰、明杠）。
- **绝张** = 用户假设（四栏中的"绝张"即假设和最后一张）；算番器不自判。
- **边／坎／钓**：仅当整手独听一种牌时才计 1 番。
- 花牌不出现（不参与任何计算）。

## 四、线框示意
```
┌────────────────────────────────────────────────────┐
│  副露区                     │      手牌区            │
│  [碰 5条×3]                 │  1万 2万 3万 4万       │
│  [吃 2万 3万 4万]           │  5万 6万 7筒 8筒       │
│                             │  9筒 东                │
├────────────────────────────────────────────────────┤
│  万  [1][2][3][4][5][6][7][8][9]   │ 圈风 东南西北 │
│  条  [1][2][3][4][5][6][7][8][9]   │ 门风 东南西北 │
│  筒  [1][2][3][4][5][6][7][8][9]   │               │
│  字  [东][南][西][北][中][发][白]  │               │
├────────────────────────────────────────────────────┤
│  东   点和 6 ｜ 自摸 8 ｜ 点和绝张 10 ｜ 自摸绝张 12 │
│  ...                                               │
└────────────────────────────────────────────────────┘
```

## 五、待办
- [x] 结果区对齐：表头与数据列对齐偏差（已修）。

## 六、已定口径
1. 番种明细：常驻显示，只列牌型番，不含和牌方式类；无番和只显示"无番和"。
2. 副露录入：先选类型（吃/碰/明杠/暗杠）再点牌，按定义校验。
3. 结果区排序：无要求。
