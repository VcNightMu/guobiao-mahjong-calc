# 国标麻将算番器 —— 听牌算番方案 v0.1

## 1. 范围（已锁定）
- 只算**手牌番**；花牌完全不参与，既不计番也不参与起和。
- 不计**偶然番**：抢杠和、海底捞月、妙手回春、杠上开花。
- 和牌方式三种：**常态（点炮）／自摸／绝张**。
- **无番和**：计入。0 番副露手按无番和 8 番。
- **门清（副露区为空）自摸 → 不求人 4 番**，不再另加自摸。
- 副露区：吃／碰／杠都录，杠区分明杠、暗杠（补杠归明杠）。

## 2. 数据模型
牌索引（34 种）：
```
万 0..8   条 9..17   筒 18..26
东 27  南 28  西 29  北 30   中 31  发 32  白 33
```
输入：
- `hand`：手牌（听牌态 13 张），`counts[34]`
- `melds`：`[{kind: chi|pon|kan, tiles, open: bool}]`
  - 吃 → chi(明)；碰 → pon(明)；明杠 → kan(明)；暗杠 → kan(暗)；补杠 → kan(明)
  - 杠占一个面子位但用 4 张
- `roundWind` / `seatWind`：东南西北
- 每个候选牌：绝张前提开关（用户勾选）

## 3. 主流程
```
wait_tiles = []                       # 先算完整听牌集
for T in 34 candidates (count < 4):
    h14 = hand + T
    if not winning(h14): continue
    wait_tiles.append(T)

for T in wait_tiles:
    best = {normal:-1, tsumo:-1, last:-1}
    for shape in decompositions(h14):
        base = recognize(shape, melds, T, roundWind, seatWind)  # 手牌番，不含门清/和牌方式
        base = applyExclusion(base)
        # 边/坎/钓 仅当“整手独听这一种牌”
        if len(wait_tiles) == 1 and T matches 边/坎/钓:
            base += 1
        normal = base + (menqing ? 2 : 0)      # 门前清
        tsumo  = base + (menqing ? 4 : 1)      # 不求人 / 自摸
        last   = base + (menqing ? 2 : 0) + 4  # 门前清 + 和绝张
        # 无番和：base==0 且非门清 且该方式本身不引入番
        if base == 0 and not menqing:
            normal = max(normal, 8)
        best.normal = max(best.normal, normal)
        best.tsumo  = max(best.tsumo, tsumo)
        best.last   = max(best.last, last)
    # 展示
    show T with {normal, tsumo, last}
```

## 4. 和牌型判定
1. 基本型：4 面子 + 1 将（面子含副露）
2. 七对（门清）
3. 十三幺（门清）
4. 全不靠 / 七星不靠

## 5. 情境番规则表（和牌方式类，单独叠加）
| 门清? | 方式 | 加番 |
|---|---|---|
| 是 | 常态 | 门前清 +2 |
| 是 | 自摸 | 不求人 +4（不含自摸）|
| 否 | 常态 | +0 |
| 否 | 自摸 | 自摸 +1 |
| 任意 | 绝张 | 常态基础上再 +和绝张 4 |

## 6. 关键陷阱
1. **无番和只在常态成立**。base=0 且副露时，常态按无番和 = 8；但自摸(+1)、绝张(+4)会引入番，破坏“0 番”条件，那些方式下反而 < 8，不能和。
2. **边/坎/钓依赖“独听”**：要求整手牌只听这一种牌才计 1 番。所以必须先算完整听牌集，候选之间不独立。
3. **绝张靠外部信息**（牌河/明面），算番器只能按用户假设，不能自判。
4. **不重复原则 = 两件事**：① 拆法搜索取最大；② 结构性互斥查表。后者命中必须扣，别漏。
5. 起和判定非单调：`{0 → 无番和 8} ∪ {≥8}` 能和，`1..7` 卡死。
6. **暗杠不破门清**：判门清只看是否吃、碰、明杠；有暗杠仍算门清，门前清/不求人照样成立。

## 7. 手牌番清单（排除花牌 / 和牌方式类 / 门清类）
- **88** 大四喜 大三元 绿一色 九莲宝灯 四杠 连七对 十三幺
- **64** 清幺九 小四喜 小三元 字一色 四暗刻 一色双龙会
- **48** 一色四同顺 一色四节高
- **32** 一色四步高 三杠 混幺九
- **24** 七对 七星不靠 全双刻 清一色 一色三同顺 一色三节高 全大 全中 全小
- **16** 清龙 三色双龙会 一色三步高 全带五 三同刻 三暗刻
- **12** 全不靠 组合龙 大于五 小于五 三风刻
- **8** 花龙 推不倒 三色三同顺 三色三节高 无番和(特殊)
- **6** 碰碰和 混一色 三色三步高 五门齐 全求人 双暗杠 双箭刻
- **4** 全带幺 双明杠
- **2** 箭刻 圈风刻 门风刻 平和 四归一 双同刻 双暗刻 暗杠 断幺
- **1** 一般高 喜相逢 连六 老少副 幺九刻 明杠 缺一门 无字 边张 坎张 单钓将

（和牌方式类另计：门前清2 自摸1 不求人4 和绝张4）
（全求人依赖荣和+副露+单钓，属半情境番，需单独处理）

## 8. 互斥表（不重复原则，部分）
| 番种 | 不计 |
|---|---|
| 大四喜 | 三风刻 碰碰和 圈风刻 门风刻 幺九刻 |
| 大三元 | 双箭刻 箭刻 |
| 绿一色 | 混一色（可计清一色）|
| 九莲宝灯 | 清一色 门前清 幺九刻 |
| 四杠 | 单钓将 |
| 连七对 | 清一色 门前清 单钓将 |
| 十三幺 | 五门齐 门前清 单钓将 |
| 清幺九 | 碰碰和 全带幺 幺九刻 无字 |
| 小四喜 | 三风刻 幺九刻 |
| 小三元 | 双箭刻 箭刻 |
| 字一色 | 碰碰和 全带幺 幺九刻 |
| 四暗刻 | 碰碰和 门前清（及三暗刻/双暗刻）|
| 一色双龙会 | 七对 清一色 平和 一般高 老少副 |
| 一色四同顺 | 一色三节高 一色三同顺 七对 四归一 一般高 |
| 一色四节高 | 一色三同顺 碰碰和 |
| 一色四步高 | 连六 老少副 |
| 混幺九 | 碰碰和 全带幺 幺九刻 |
| 七对 | 门前清 单钓将 |
| 七星不靠 | 五门齐 门前清 |
| 全双刻 | 碰碰和 断幺 |
| 清一色 | 无字 |
| 一色三同顺 | 一色三节高 一般高 |
| 一色三节高 | 一色三同顺 |
| 全大 / 全小 | 无字 |
| 全中 | 断幺 无字 |
| 三色双龙会 | 平和 无字 喜相逢 老少副 |
| 全带五 | 断幺 无字 |
| 推不倒 | 缺一门 |
| 三色三同顺 | 喜相逢 |
| 全求人 | 单钓将 |
| 双箭刻 | 箭刻 |
| 大于五 / 小于五 | 无字 |

## 9. 展示规则
每个和牌张输出：番种明细 + 常态/自摸/绝张三种是否够 8。
- 够 8 的方式正常显示。
- 只有某方式够 → 标注“仅自摸可达”／“仅和绝张可达”。
- 都够不到 → 不显示；或单独归入“结构听牌但起和不足”（有价值信息，可选提示）。
